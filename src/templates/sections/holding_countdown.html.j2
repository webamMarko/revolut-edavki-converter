<section class="card" id="holdingCountdownSection" style="display:none">
  <div class="chart-header">
    <h2 id="holdingCountdownTitle">Holding Period Countdown</h2>
  </div>
  <p id="holdingCountdownDesc" style="color:var(--muted);font-size:0.8rem;margin:-0.5rem 0 1rem"></p>

  <!-- Upcoming milestones highlight -->
  <div id="holdingCountdownHighlight" class="card-grid small"></div>

  <!-- Per-lot countdown table -->
  <div class="table-scroll" style="margin-top:1rem">
    <table id="holdingCountdownTable" class="data-table sortable"></table>
  </div>
</section>
